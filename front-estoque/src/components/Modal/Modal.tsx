

type ModalProps = {
    id : string,
    name: string,
    description: string,
    image: string,
    mode: string
}

export default function Modal({id, name, description, image, mode} : ModalProps){
    
}